var canvas = document.getElementById('myCanvas');

const leftButtonX = canvas.width * 0.05;
const leftButtonY = canvas.height * 0.9;
const rightButtonX = canvas.width * 0.25;
const rightButtonY = canvas.height * 0.9;
const topButtonX = canvas.width * 0.15;
const topButtonY = canvas.height * 0.85;
const bottomButtonX = canvas.width * 0.15;
const bottomButtonY = canvas.height * 0.95;
const mainGeneralStartingX = canvas.width * 0.05;
const mainGeneralStartingY = canvas.height * 0.05;
const mainGeneralSecondX = canvas.width * 0.55;

export default {
  leftButtonX,
  leftButtonY,
  rightButtonX,
  rightButtonY,
  topButtonX,
  topButtonY,
  bottomButtonX,
  bottomButtonY,
  mainGeneralStartingX,
  mainGeneralStartingY,
  mainGeneralSecondX,
};
